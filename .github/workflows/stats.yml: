name: Generate GitHub Stats

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  github-stats:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Generate GitHub Stats
        uses: jstrieb/github-stats@master
        with:
          username: BurukalaManiReethika
          access_token: ${{ secrets.ACCESS_TOKEN }}
